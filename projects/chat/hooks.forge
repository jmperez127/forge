# Chat Hooks

hook Message.after_create {
  enqueue notify_channel
  enqueue log_message_sent
}

hook Thread.after_create {
  enqueue notify_parent_author
}

hook Workspace.after_create {
  enqueue create_default_channel
}
